@inject IJSRuntime JSRuntime

@inject IScopeManager ScopeManager;

@code{
    [Parameter]
    public string Value { get; set; }

    [Parameter]
    public string AppName { get; set; } = $"DevCoreApp";

    [Parameter]
    public string Format { get; set; } = "{1} · {0}";
}
